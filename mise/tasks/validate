#!/usr/bin/env bash
#MISE description="Validate BST element graph — verify key stacks and OCI pipeline resolve"

set -euo pipefail
./mise/tasks/bst show --deps all stacks/base-system.bst
./mise/tasks/bst show --deps all stacks/bootc.bst
./mise/tasks/bst show --deps all oci/krytis/image.bst
