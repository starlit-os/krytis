name: Publish krytis image

on:
  workflow_dispatch:
    inputs:
      report_only:
        description: 'Report vulnerabilities without blocking the run (default: block on Critical)'
        type: boolean
        default: false
      allow_branch_publish:
        description: 'Permit publishing from a ref other than main (CI testing). The resulting :latest will NOT satisfy the strict main-pinned verification policy — re-publish from main afterwards.'
        type: boolean
        default: false

permissions:
  contents: read
  packages: write
  id-token: write

concurrency:
  group: krytis-publish
  cancel-in-progress: false

jobs:
  publish:
    # Blacksmith to start (#380) — no self-hosted fallback input yet. If
    # Blacksmith proves unfeasible (disk/time), swap this to
    # ["self-hosted","linux","x64"] (docs/skills/ci-runner.md); nothing
    # else in this job is Blacksmith-specific.
    runs-on: blacksmith-8vcpu-ubuntu-2404
    timeout-minutes: 420
    steps:
      - name: Gate on ref (only main may publish)
        # FIRST, before build or push. `mise run push` always tags :latest and
        # runs at step ~9, while signature verification runs at ~13 — so a
        # main-pinned verify alone would publish :latest publicly and only then
        # fail. Refusing here means a wrong-ref dispatch costs nothing and the
        # error names the actual cause instead of surfacing as a signature
        # problem.
        #
        # allow_branch_publish exists because testing changes to this workflow
        # requires dispatching it from a branch (that is how #421 itself was
        # validated). It is opt-in and loud rather than the default.
        run: |
          if [[ "${{ github.ref_name }}" == "main" ]]; then
            echo "::notice::Publishing from main — signatures will satisfy the strict verification policy"
            exit 0
          fi
          if [[ "${{ inputs.allow_branch_publish }}" != "true" ]]; then
            echo "::error::Refusing to publish from '${{ github.ref_name }}'. Only main may publish."
            echo "  Nothing has been built or pushed."
            echo "  To publish from a branch anyway (CI testing), re-dispatch with allow_branch_publish=true."
            exit 1
          fi
          echo "::warning::Publishing from '${{ github.ref_name }}', not main. \
          The resulting ghcr.io/starlit-os/krytis:latest will be signed with a \
          non-main identity and will NOT verify against the strict policy in \
          docs/skills/signing.md. Re-publish from main afterwards."

      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Maximize build space
        # This pinned SHA's action.yml only accepts `extra-squeeze` (verified
        # against ublue-os/remove-unwanted-software@695eb75... directly) — the
        # remove-dotnet/remove-android/remove-haskell/remove-codeql inputs
        # cache-warm.yml uses predate this pin and are silently ignored
        # (composite actions warn, not error, on unrecognized inputs). See #389.
        uses: ublue-os/remove-unwanted-software@695eb75bc387dbcd9685a8e72d23439d8686cba6 # v10
        with:
          extra-squeeze: "true"

      - name: Setup mise
        uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654 # v4.2.4
        with:
          experimental: true

      - name: Install system dependencies
        run: mise bootstrap --yes

      - name: Install Python dependencies
        run: uv sync

      - name: Enable unprivileged user namespaces
        run: sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0 || true

      - name: Scan for vulnerabilities (pre-build gate)
        # Runs before the build so a blocking finding aborts here — no BST
        # build time spent, and mise/tasks/push's own scan-before-push
        # (below) is the defense-in-depth guarantee that nothing gets
        # pushed even if this early gate is ever skipped/bypassed (#392).
        # buildstream-sbom reads static element/source metadata via
        # `bst show` — it does not require the image to be built.
        id: vuln-scan
        run: |
          if [[ "${{ inputs.report_only }}" == "true" ]]; then
            echo "::notice::report_only=true — vulnerability scan will not block the build"
            mise run vuln-scan
          else
            mise run vuln-scan --fail-on critical
          fi

      - name: Build oci/krytis/image.bst
        env:
          BUILDBARN_PULL_TOKEN: ${{ secrets.BUILDBARN_PULL_TOKEN }}
        run: mise run build --pull

      - name: Push, attach SBOM + vulnerability report
        id: push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [[ "${{ inputs.report_only }}" == "true" ]]; then
            echo "::notice::report_only=true — vulnerability scan will not block this run"
            mise run push
          else
            mise run push --fail-on critical
          fi

      - name: Sign image and referrers with cosign (best-effort)
        id: sign
        # Runs regardless of whether either severity gate above tripped.
        # If the pre-build gate or push's own scan-before-push blocked
        # publish, no image was ever pushed — this step then has nothing
        # to sign and cleanly no-ops (continue-on-error absorbs its exit).
        # continue-on-error: signing failure never fails this job (#380
        # design decision: parallel/best-effort, matching dakota's
        # publish-sbom job). A transient Fulcio/Rekor outage must not block
        # promotion of an image that is otherwise fine.
        if: always()
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: mise run sign

      - name: Verify signature
        # BLOCKING, but only when signing itself reported success (#418).
        #
        # Previously this was continue-on-error too, which meant nothing in the
        # pipeline ever rejected a bad signature — we signed and then ignored
        # whether the signature was verifiable with our own policy. That is the
        # gap #418 closes.
        #
        # Gating on steps.sign.outcome == 'success' rather than making this
        # unconditionally blocking is deliberate: it preserves #380's outage
        # tolerance above. Sign failed (Fulcio/Rekor down, or nothing was
        # pushed) -> this is skipped and publish still succeeds. Sign claimed
        # success but the signature cannot be verified against the identity we
        # expect -> the job fails, which is the only case where a signature
        # problem is genuinely ours.
        if: always() && steps.sign.outcome == 'success'
        run: |
          if [[ ! -f krytis-push-digests.env ]]; then
            # sign reported success with no digest file: it no-op'd because
            # nothing was pushed. Nothing to verify, and not a failure.
            echo "::notice::No digest file — nothing was published, so no signature to verify"
            exit 0
          fi
          # shellcheck disable=SC1091
          source krytis-push-digests.env
          verify() {
            local label="$1" digest="$2"
            [[ -z "${digest}" ]] && return 0
            echo "==> Verifying ${label} (${digest})"
            # Pin the signature to publish.yml in THIS repo at THIS ref. The
            # previous `@refs/heads/.*` accepted a signature from any branch,
            # so the check proved "some branch of this repo signed it" rather
            # than naming one — and the run that validated #421 was itself a
            # branch publish, which is how that was noticed.
            #
            # github.ref_name resolves to `main` on the normal path, so this IS
            # main-pinning there; on an opt-in branch publish it pins to that
            # branch, which is still tighter than the old wildcard. The "only
            # main may publish" policy is enforced by the ref gate at the top of
            # this job, not here — one job, one place.
            cosign verify \
              --certificate-identity-regexp "^https://github\.com/${{ github.repository }}/\.github/workflows/publish\.yml@refs/heads/${{ github.ref_name }}\$" \
              --certificate-oidc-issuer https://token.actions.githubusercontent.com \
              "ghcr.io/starlit-os/krytis@${digest}"
          }
          # Verify every artifact sign signed, not just the image — an unverifiable
          # SBOM or vulnerability-report signature is the same class of problem.
          verify "image" "${image_digest:-}"
          verify "SBOM" "${sbom_digest:-}"
          verify "vulnerability report" "${vuln_digest:-}"

      - name: Report severity gate outcome
        if: always()
        run: |
          if [[ "${{ steps.push.outcome }}" == "success" ]]; then
            echo "::notice::Publish succeeded (severity gate: $([[ '${{ inputs.report_only }}' == 'true' ]] && echo 'report-only' || echo 'blocking at Critical'))"
          elif [[ "${{ steps.vuln-scan.outcome }}" == "failure" ]]; then
            echo "::error::Vulnerability scan blocked this run before the build started — see the 'Scan for vulnerabilities (pre-build gate)' step above"
            exit 1
          else
            echo "::error::Publish step failed — see logs above (push's own scan-before-push re-check, or a build/push failure)"
            exit 1
          fi
