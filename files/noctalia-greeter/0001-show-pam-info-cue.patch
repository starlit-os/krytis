--- a/src/greeter/greeter_surface.cpp
+++ b/src/greeter/greeter_surface.cpp
@@ -1042,4 +1042,4 @@ void GreeterSurface::layoutScene(std::uint32_t width, std::uint32_t height) {
-  const bool hasStatus = m_statusIsError && !m_status.empty();
+  const bool hasStatus = !m_status.empty();
   const std::string actualStatus = m_status;
   m_statusLabel->setText("Xy");
   m_statusLabel->measure(*renderer);
@@ -1238,8 +1238,9 @@ bool GreeterSurface::driveAuthConversation(std::optional<GreetdAuthMessage> pend
     if (authMsg.type == GreetdAuthMessageType::Info || authMsg.type == GreetdAuthMessageType::Error) {
-      if (authMsg.type == GreetdAuthMessageType::Error) {
-        updateStatus(authMsg.message, true);
-        clearPasswordInput();
-      }
+      const bool isError = authMsg.type == GreetdAuthMessageType::Error;
+      updateStatus(authMsg.message, isError);
+      if (isError) {
+        clearPasswordInput();
+      }
       pending = m_greetdClient->postAuthData("");
       if (m_greetdClient->lastError()) {
         onAuthError(*m_greetdClient->lastError());
@@ -1367,10 +1368,10 @@ void GreeterSurface::onAuthError(const GreetdError& error) {
 void GreeterSurface::updateStatus(const std::string& text, bool isError) {
   if (!isError) {
-    m_status.clear();
+    m_status = text;
     m_statusIsError = false;
     if (m_statusLabel != nullptr) {
-      m_statusLabel->setText("");
+      m_statusLabel->setText(text);
     }
     requestLayout();
     return;
   }
