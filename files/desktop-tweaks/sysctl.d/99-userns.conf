# Allow unprivileged user namespace creation.
# Required by Flatpak's bubblewrap sandbox (and Chromium/Electron sandbox).
# CachyOS ships with this disabled; re-enable it so Flatpak app sandboxes work.
kernel.unprivileged_userns_clone=1
